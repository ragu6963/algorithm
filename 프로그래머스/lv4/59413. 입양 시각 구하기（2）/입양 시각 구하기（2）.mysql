WITH recursive TEMP as (
    SELECT 0 as HOUR
    UNION ALL
    SELECT HOUR + 1 FROM TEMP
    WHERE HOUR < 23
)
# SELECT HOUR FROM TEMP
SELECT A.HOUR, COUNT(HOUR(B.DATETIME)) FROM TEMP A
LEFT JOIN ANIMAL_OUTS B
ON A.HOUR = HOUR(B.DATETIME)
GROUP BY A.HOUR